server:
  port: 8762
spring:
  application:
    name: nativeconfigserver
  profiles:
    active: native #配置文件的获取方式 （本地）
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared #本地配置文件存放的路径，classpath代表配置文件的根目录（resources文件夹）